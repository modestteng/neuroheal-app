# 智愈莘莘 NeuroHeal App 后续设计工作流

> 本文只讨论 App 本身接下来怎么设计、怎么改、怎么实现。它不是计划书、申报书、PPT 或 Word 文档写作提纲。当前项目已经申请软著，因此后续开发应在已备份版本基础上增量新增，尽量不破坏原有代码结构、软著材料和既有页面逻辑。

## 0. 版本保护

- 已完成完整备份：`D:\比赛\design\neuroheal-app_backup_before_workflow_20260614_214140`
- 后续改动只围绕 App 页面、组件、数据结构和交互流程展开。
- 不删除 `软著材料`、`软件著作注意事项`、已有截图和已有说明书。
- 新增功能优先采用“新增页面 / 新增组件 / 新增数据文件”的方式，少改原有核心文件。

## 1. 当前 App 基线

当前 App 已经具备一个完整的学生端演示闭环：

- `监测`：首页展示 EEG 波形、Valence 情绪效价、置信度、信号质量、AI 反馈和推荐干预。
- `干预`：根据 Valence 状态推荐呼吸训练、数字处方、意念赛车、AI 陪伴等行动。
- `记录`：展示闭环记录、打卡、勋章、成长档案和心理周报。
- `设备`：展示 NeuroBand 头环连接、电极质量、采样率、校准流程。
- `AI 陪伴`：通过 `/api/chat` 调用后端代理，结合 Valence 上下文生成回复。
- `小游戏`：意念赛车通过交互模拟专注训练，并记录单局结果。
- `数字处方`：已有专注、助眠、情绪复盘、社交自信等训练包。

当前主要问题：

- App 的主线已经有，但“产品核心亮点”不够集中。
- 当前页面更像功能堆叠，缺少让用户一眼看懂”软硬件闭环”完整路径的入口。
- 硬件技术链路、算法链路和干预后再监测的关系还可以更直观。
- 数据大多是 mock，后续逐步用组件内状态联动替换静态数据。

## 2. App 设计总目标

后续 App 不重做，而是在现有基础上强化四件事：

1. 打开 App 后立刻看懂：这是”EEG 脑电 + AI 干预 + 高校心理健康”的闭环系统。
2. 操作路径更清楚：监测异常 -> AI 解释 -> 推荐干预 -> 完成训练 -> 再监测 -> 形成记录。
3. 技术链路可见：头环参数、信号质量、校准、算法识别、置信度、干预前后 Valence 变化在页面里直观展示。
4. 数据真实联动：干预、监测、记录三个模块共享同一状态源，不各自依赖静态 mock。

## 3. 推荐 App 信息架构

保留当前底部三栏结构，但在每个栏目里补强重点。

| 底部栏目 | 当前定位 | 后续设计重点 |
| --- | --- | --- |
| 监测 | EEG-Valence 控制台 | 增加核心亮点、软硬件闭环图、异常提醒、下一步行动 |
| 干预 | AI 推荐行动中心 | 强化“为什么推荐这个干预”、干预完成后的反馈 |
| 记录 | 闭环记录与成长档案 | 增加干预前后对比、趋势、市场/使用数据入口 |

建议新增的子页面：

- `技术路线页`：展示硬件采集、信号处理、Valence 识别、AI 反馈、干预闭环。
- `数据看板页`：展示 App 使用数据、训练完成次数、Valence 变化、设备连接数据。
- `产品展示页`：展示 App 核心功能、视频二维码、硬件图和下载/注册数据。
- `心理中心页`：模拟学校心理中心视角，展示授权后的预警、随访和群体趋势。

这些页面不一定都放进底部导航，可以作为首页或记录页里的入口卡片。

## 4. 核心用户流程

### 流程一：首次进入 App

目标：用户能快速理解产品，并进入监测。

建议流程：

1. 首页显示产品名、当前设备连接状态、核心闭环一句话。
2. 显示 NeuroBand 头环连接状态。
3. 引导用户进入设备校准。
4. 校准完成后进入实时监测控制台。
5. 首页显示当前 Valence 状态和 AI 建议。

需要优化的点：

- 当前首页可以增加一个“闭环总览卡片”，把头环、AI、干预、记录串起来。
- 不要放太多说明文字，用图标、流程线和状态标签表达。

### 流程二：实时监测到低效价

目标：让用户知道系统发现了什么，以及下一步该做什么。

建议流程：

1. EEG 波形持续更新。
2. Valence 从较高效价变为较低效价。
3. App 显示“状态有波动，建议先进行短时调节”。
4. AI 生成 2-3 句温和反馈。
5. 推荐一个最合适的干预，例如呼吸训练或 AI 陪伴。
6. 用户点击开始。

需要优化的点：

- 推荐卡片里增加“推荐原因”。
- 异常状态不要用吓人的红色大面积警告，保持温和提醒。
- 明确说明这是辅助判断，不是医学诊断。

### 流程三：完成一次干预

目标：让用户看到干预前后变化，形成闭环感。

建议流程：

1. 用户进入呼吸训练、意念赛车或数字处方。
2. 训练过程展示进度、实时放松度/专注度。
3. 训练完成后进入结果页。
4. 结果页显示干预前 Valence、干预后 Valence、变化值。
5. 点击“返回并继续监测”。
6. 记录页自动新增一条闭环记录。

当前短板：

- 呼吸训练完成后只是显示放松度提升，没有真正和 `closedLoopRecords` 联动。
- 意念赛车只记录本局专注表现，没有写入 Valence 闭环结果。

后续实现建议：

- 在 `ValenceLoopProvider` 中增加 `completeIntervention(actionId, result)` 方法。
- 干预完成后更新当前 Valence 快照和闭环记录。
- 记录页读取同一个状态源，而不是完全依赖静态 mock。

### 流程四：长期记录与随访

目标：让 App 不只是一次性演示，而是体现持续使用价值。

建议流程：

1. 用户每日完成监测或训练。
2. App 自动记录 Valence 趋势、训练次数、完成率。
3. 成长档案生成周报。
4. 用户可选择授权给学校心理中心查看部分数据。
5. 心理中心页展示预警、随访状态和群体趋势。

需要注意：

- 授权必须显式展示，不能默认共享。
- App 内要写清楚“仅授权后可见”。
- 学生端只展示个人数据，心理中心页可以作为演示端入口。

## 5. 页面改造工作流

### 第一步：重构首页首屏

目标：让 App 第一屏更像成熟产品，而不是散落的功能卡片。

建议增加：

- 顶部状态条：设备在线、信号质量、今日监测时长。
- 核心亮点卡片：EEG 采集、Valence 识别、AI 干预、闭环记录。
- 软硬件闭环图：头环 -> App -> AI -> 干预 -> 再监测。
- 当前状态卡片：保留 Valence 分数、等级、置信度、EEG 波形。
- 推荐行动卡片：突出“开始干预”按钮。

涉及文件：

- `src/screens/HomeScreen.tsx`
- 可新增 `src/components/LoopOverview.tsx`
- 可新增 `src/components/ProductHighlights.tsx`

### 第二步：增强设备页

目标：让硬件部分更可信。

当前处理原则：

- 硬件真实图片、参数、采集证明、视频先不填假内容。
- 已预留 `src/data/hardware.ts` 作为硬件资料接口。
- 后续拿到材料后，优先只补 `hardware.ts` 里的 `src`、`thumbnail`、`value` 等字段，再根据需要微调页面样式。

建议增加：

- 头环硬件参数卡片：采样率、电极数量、连接方式、续航、固件版本。
- 信号链路卡片：采集 -> 滤波 -> 特征提取 -> Valence 识别。
- 佩戴质量提示：电极接触不足时提示调整。
- 校准结果页：完成后显示“基线已更新”“识别模型已适配当前佩戴状态”。

涉及文件：

- `src/screens/sub/DeviceScreen.tsx`
- `src/data/mock.ts`
- `src/data/hardware.ts`
- 后续如硬件数据变多，可再拆出 `src/data/device.ts`

### 第三步：增强干预页

目标：让用户清楚知道为什么推荐这个训练。

建议增加：

- 每个干预方式增加适用状态：
  - 较低效价：呼吸训练、AI 陪伴。
  - 注意力下降：意念赛车。
  - 状态稳定：数字处方和专注训练。
- 推荐理由更结构化：
  - 当前状态
  - 推荐目标
  - 预计时长
  - 完成后会记录什么
- 干预完成后统一进入结果页。

涉及文件：

- `src/screens/TrainScreen.tsx`
- `src/screens/sub/PlayerScreen.tsx`
- `src/screens/sub/RaceScreen.tsx`
- `src/screens/sub/PrescriptionScreen.tsx`
- `src/state/useValenceLoop.tsx`

### 第四步：建立统一闭环状态

目标：减少静态 mock，让干预、监测、记录之间真正联动。

建议新增状态能力：

```ts
type InterventionResult = {
  actionId: string;
  startedAt: string;
  beforeScore: number;
  afterScore: number;
  beforeLevel: ValenceLevel;
  afterLevel: ValenceLevel;
  durationSec: number;
  metrics: Record<string, number>;
};
```

需要实现：

- `startIntervention(actionId)`：记录开始前状态。
- `completeIntervention(result)`：生成干预结果。
- `appendLoopRecord(record)`：写入闭环记录。
- `getRecommendedAction(current)`：统一管理推荐逻辑。

建议文件：

- 扩展 `src/state/useValenceLoop.tsx`
- 可新增 `src/state/loopTypes.ts`
- 可新增 `src/utils/valence.ts`

### 第五步：新增数据看板页

目标：把用户个人的监测和训练数据可视化，帮助用户了解自己的情绪规律和干预效果。

建议展示两个区块：

**个人数据区**（来自用户本地闭环记录）：

- 今日监测次数。
- 本周训练完成次数。
- 平均 Valence 变化趋势。
- 各类干预完成率（呼吸训练 / 意念赛车 / 数字处方）。
- 设备连接成功率。
- 支持”今日 / 本周 / 本月”数据切换。

**平台概览区**（展示平台整体规模，数据来自 `metrics.ts`）：

- 累计注册用户数。
- App 下载量。
- 全平台训练完成次数。
- 已接入试点学校数。

涉及文件：

- 可新增 `src/screens/sub/DataDashboardScreen.tsx`
- 可新增 `src/data/metrics.ts`
- 记录页 `CommunityScreen.tsx` 增加入口。

### 第六步：新增产品介绍页

目标：为新用户提供一个能快速了解 App 核心能力、主要功能入口和使用方式的页面。

建议模块：

- 产品功能一句话简介。
- 软硬件闭环流程示意图：头环 -> App -> AI -> 干预 -> 再监测。
- 核心功能入口快捷导航：监测、设备、干预、记录、心理中心。
- 产品演示视频二维码卡片（扫码即可观看使用演示）。
- 头环图和主要硬件参数卡片。

涉及文件：

- 可新增 `src/screens/sub/ProductIntroScreen.tsx`
- 可新增 `src/components/QRCodeCard.tsx`

### 第七步：模拟心理中心端

目标：让 App 体现高校落地场景，而不是只停留在个人情绪工具。

建议做成一个演示子页面：

- 今日授权学生数。
- 低效价连续波动提醒。
- 已完成干预人数。
- 待随访列表。
- 群体 Valence 趋势。
- 隐私授权说明。

注意：

- 不展示真实个人隐私。
- 学生姓名用匿名编号。
- 页面顶部写清楚“仅展示已授权数据”。

涉及文件：

- 可新增 `src/screens/sub/CounselorScreen.tsx`
- 可新增 `src/data/counselor.ts`

## 6. 数据结构调整建议

当前 `src/data/mock.ts` 已经偏大，后续建议拆分。

建议拆分为：

- `src/data/user.ts`：用户、等级、打卡。
- `src/data/hardware.ts`：硬件图片、参数、视频、采集证明的预留接口。
- `src/data/device.ts`：后续硬件数据变多时，可拆出的设备、电极、校准数据。
- `src/data/valence.ts`：Valence 快照、等级、推荐逻辑示例。
- `src/data/interventions.ts`：呼吸、数字处方、AI 陪伴、意念赛车。
- `src/data/metrics.ts`：平台概览数据（下载量、注册数、全平台训练次数、试点学校数）。
- `src/data/counselor.ts`：心理中心演示数据。

拆分原则：

- 先新增文件，不急着大规模迁移。
- 新模块优先读新数据文件。
- 旧页面可以暂时继续读 `mock.ts`，避免一次改动太大。

## 7. UI 设计原则

- 主色继续保持清爽、舒缓、稳定，不做过度刺激的医疗红色。
- 心理健康状态提醒用温和提示，不制造恐慌。
- 每个页面只突出一个主任务。
- 按钮文案要像真实 App：
  - “开始调节”
  - “查看原因”
  - “继续监测”
  - “生成记录”
  - “授权给心理中心”
- 卡片不要堆太多文字，优先用状态、指标、流程线、图标表达。
- 低效价状态也要给用户安全感，不要显示“危险”“异常严重”等绝对化词语。

## 8. 交互细节清单

需要补的关键交互：

- 设备校准完成后，自动返回监测页或显示“进入监测”按钮。
- AI 反馈请求失败时，显示本地兜底建议。
- 干预开始时锁定干预前 Valence。
- 干预结束时生成结果页。
- 结果页点击“继续监测”后更新首页状态。
- 记录页新增最近一次闭环记录。
- 数据看板支持“今日 / 本周 / 本月”切换。
- 产品展示页二维码支持替换链接。
- 心理中心页的数据必须显示授权状态。

## 9. 开发优先级

### P0：先做，直接提升 App 完整度

- 首页首屏重构：核心亮点 + 闭环总览 + 推荐行动。
- 设备页增强：硬件参数 + 信号处理链路。
- 干预完成结果页。
- 统一闭环状态：干预前后数据能写入记录页。
- 产品介绍页：功能导航、闭环示意图和演示视频二维码。

### P1：第二轮做，完善数据联动与演示场景

- 数据看板页。
- 心理中心演示页。
- 记录页趋势图增强。
- 训练推荐逻辑独立成工具函数。
- mock 数据拆分。

### P2：有时间再做，偏真实产品化

- 登录和用户体系。
- 后端存储闭环记录。
- 真实硬件接口适配。
- 真实统计接口。
- 权限管理和隐私授权流程。
- 心理中心管理后台独立页面。

## 10. 推荐实施顺序

1. 保留当前代码，先新增 App 专用数据文件：`hardware.ts`、`metrics.ts`、`counselor.ts`。
2. 新增首页亮点组件和闭环总览组件。
3. 改造 `HomeScreen.tsx`，让第一屏更集中。
4. 改造 `DeviceScreen.tsx`，补硬件参数和信号链路。
5. 扩展 `useValenceLoop.tsx`，加入干预开始和完成方法。
6. 改造 `PlayerScreen.tsx` 和 `RaceScreen.tsx`，完成后写入闭环记录。
7. 改造 `CommunityScreen.tsx`，显示真实联动后的闭环记录。
8. 新增 `ProductIntroScreen.tsx`，放功能导航、闭环示意图和演示视频二维码。
9. 新增 `DataDashboardScreen.tsx`，展示使用与效果指标。
10. 新增 `CounselorScreen.tsx`，模拟高校心理中心已授权数据视角。

## 11. 验收标准

完成后，App 应该满足：

- 打开首页能一眼看出 EEG、Valence、AI、干预闭环。
- 设备页能体现硬件存在感和技术链路。
- 用户可以从低效价状态进入干预，并看到干预前后变化。
- 干预结果能进入记录页，不只是页面内临时显示。
- 数据看板能展示个人监测和训练效果指标，支持时间维度切换。
- 产品介绍页能导航到各功能，并展示演示视频二维码。
- 心理中心页能体现高校落地场景和授权查看逻辑。
- 所有医学表述保持“辅助、建议、预警、干预支持”，不写诊断和治疗承诺。

## 12. 一句话方向

接下来只围绕 App 做增量设计：把现有的监测、干预、记录三个栏目打通成真正联动的闭环，再补上设备技术链路页、用户数据看板、产品介绍页和心理中心演示页，让用户打开 App 就能看到完整的功能路径、技术细节和落地场景。
