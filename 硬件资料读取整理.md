# ExpandableEEG 硬件资料读取整理

资料来源：`C:\Users\崔家腾\Desktop\ExpandableEEG files.zip`

说明：该路径当前是一个文件夹，不是普通压缩包。核心资料分为 `EEG files` 和 `实体佩戴图` 两部分；其中 `OpenBCI`、`BrainFlow`、`Processing`、`Wireshark` 等属于支撑工具或开源依赖，不能直接写成自研成果，但可以作为“硬件接入与实验验证链路”的支撑材料。

## 1. 已确认的硬件主体

硬件名称：`ExpandableEEG`

核心说明书：`EEG files\Docs\ExpandableEEG-使用说明书.pdf`

芯片资料：`EEG files\Docs\ads1299.pdf`

实体照片：`实体佩戴图` 文件夹下 4 张实拍图，均能看到受试者佩戴黑色 EEG 脑电帽、桌面蓝色采集盒、电极线束、电脑采集/处理界面。

## 2. 硬件结构与通道能力

ExpandableEEG 的核心结构由两部分组成：

- 基板：顶层板，承担充电、主控等功能。
- 主采集板：底层板，构成基础采集能力。

通道扩展方式：

- 基础结构为 8 通道采集板。
- 加装 1 块从采集板，可扩展为 16 通道。
- 加装 3 块从采集板，可扩展为 32 通道。
- 通道扩展需要关机后安装/拆卸，操作完成后再开机，设备会自动调整采集通道数。

32 通道分层关系：

- 底层：1-8 通道。
- 第二层：9-16 通道。
- 第三层：17-24 通道。
- 第四层：25-32 通道。

可用于 APP 的表达：

> 可堆叠式 EEG 采集硬件，支持 8/16/32 通道扩展，适配从便携演示到多通道实验采集的不同场景。

## 3. 引脚、电极与多模态采集

说明书中提到的接口顺序：

- 最左侧为 `BIAS` 偏置信号。
- 从左往右依次为 8 通道到 1 通道。
- 后续为 `SRB2` 和 `SRB1`。

脑电 EEG 连接：

- 单端连接时，从采集板搭接后需要连接 `SRB` 与 `BUS_SRB`、`BIAS` 与 `BUS_BIAS`，使每个板子共享 SRB 和 BIAS。
- 默认 SRB 配置使用 `SRB2`。
- EEG 电极默认使用 `N` 通道。

肌电 EMG 连接：

- 模块支持同时采集肌电。
- EMG 通常作为双端输入，需要同时使用 `N` 和 `P` 通道。
- 此时可不连接 `SRB/BUS_SRB`、`BIAS/BUS_BIAS`。
- 连接肌电电极后，需要在 OpenBCI GUI 中配置连接方式。

可用于 APP 的表达：

> 不只采 EEG，也预留 EMG 等生理信号接入能力，后续可扩展为多模态情绪/压力评估。

## 4. 佩戴与信号质量

说明书中的佩戴要求：

- 电极需要与头皮直接良好接触。
- 佩戴前应保证头皮干燥和卫生。
- 长时间佩戴可能产生疼痛或不适。
- 佩戴后可使用 `cython signal` 控件检测电极接触区域阻抗。
- 阻抗越小，信号质量越高。
- 当前 32 通道暂无法使用阻抗检测功能。

APP 展示建议：

- 在设备页展示“佩戴状态”“电极接触质量”“阻抗/信号质量提示”。
- 对 32 通道限制要写成“当前版本 32 通道阻抗检测待完善”，不要写成已支持。

## 5. 通信方式与配置工具

ExpandableEEG 支持三种通信配置：

- 蓝牙模式：采集数据稳定性更优，续航更长。
- WiFi AP 模式：设备可充当路由器/服务端。
- WiFi Client 模式：设备像手机一样接入现有路由器。

默认配置：

- 出厂默认：`WiFi AP`
- 默认 AP 名称：`ExpandableEEG AP`
- 默认密码：`abcd1234`
- AP 模式默认 IP：`192.168.4.1`

配置方式：

- 通过 Type-C 数据线连接电脑。
- 打开 `Tools\ExpandableEEG_Config.exe`。
- 点击“连接设备”后进入配置。
- 32 通道当前暂不支持该配置工具匹配。
- Client 模式的 IP 会在调试框输出，也可启动设备时打开串口查看。
- 串口波特率：`115200`。
- 蓝牙模式下，电脑蓝牙设置会映射出 COM 口，示例为 `COM8`。

APP 展示建议：

- 首页可显示“WiFi AP / BT 双链路接入”。
- 设备页可展示“连接方式：WiFi AP、WiFi Client、蓝牙串口”。
- 配对流程可以做成 3 步：佩戴脑电帽 -> 连接 ExpandableEEG AP/蓝牙 -> 开始 EEG 数据流。

## 6. OpenBCI GUI 采集流程

使用工具：

- `Tools\openbci32\OpenBCI_GUI.exe`
- `Tools\OpenBCI_GUI\OpenBCI_GUI.exe`

WiFi 版本流程：

- 电脑连接 `ExpandableEEG AP`。
- 打开 OpenBCI GUI。
- 选择 `Cyton -> WiFi -> 根据通道数选择 CHANNEL COUNT`。
- AP 模式 IP 为 `192.168.4.1`。
- Client 模式使用配置时输出的 IP。
- 点击 `START SESSION`，进入采集界面。
- 点击 `Start Data Stream` 开始采集。

蓝牙版本流程：

- 找到映射 COM 口。
- OpenBCI GUI 选择 `Cyton -> Serial -> Manual -> COM 口`。

GUI 常用控件：

- `FFT Plot`：频谱分析。
- `Focus`：专注相关展示。
- `Networking`：向 Python、MATLAB、OpenViBE 等程序输出数据流。
- `Packet loss`：查看丢包。
- `Marker`：测试版打标。

支持命令：

- `~`
- `1`、`2`、`3`、`4`、`5`、`6`、`7`、`8`
- `!`、`@`、`#`、`$`、`%`、`^`、`&`、`*`
- `x`、`b`、`s`

验证示例：

- ADS1299 内部方波信号采集测试：用于证明芯片到 GUI 的通路正常。
- 眼电 EOG 采集测试：前额两个电极放在眉毛上方约 1cm，眨眼可观察到波形。

## 7. BrainFlow 与开发接入

说明书明确提到可以使用 BrainFlow 绕开 OpenBCI GUI，直接获取 EEG/EMG 数据、查看波形，适合开发者。

示例命令：

```bash
python -u "e:\Interesting\BCI\brainflow-master\python_package\examples\plot_real_time\plot_real_time.py" --ip-address 192.168.4.1 --board 5 --ip-port 3
```

```bash
python -u "e:\Interesting\BCI\brainflow-master\python_package\examples\plot_real_time\plot_real_time.py" --serial-port COM3 --board 0
```

32 通道注意事项：

- 需要使用 `Tools` 文件夹里的 `BoardController.dll` 替换 Python 环境中 BrainFlow 的版本。
- 替换路径示例：`...\Python312\Lib\site-packages\brainflow\lib\`
- 32 通道 `BoardId` 为 `56`。

工具目录 `readme.txt` 的原文含义：

> 已修改为 32 通道，新环境注意替换 brainflow 控件：BoardController.dll 到本机 Python 的 brainflow/lib 目录，具体路径根据 Python 环境决定。

APP 展示建议：

> 硬件信号可通过 OpenBCI GUI/LSL 或 BrainFlow 进入算法层，为 APP 的 Valence 识别、专注检测和干预推荐提供实时数据入口。

## 8. ADS1299 芯片能力

资料来源：`ads1299.pdf`

ADS1299-x 是 TI 面向 EEG 与生物电势测量的低噪声 4/6/8 通道、24 位模数转换器系列。

关键参数：

- 最多 8 个低噪声可编程增益放大器 PGA。
- 最多 8 个高分辨率同步采样 ADC。
- 输入参考噪声：`1 μVPP`，带宽 70Hz。
- 输入偏置电流：`300pA`。
- 数据速率：`250 SPS` 至 `16 kSPS`。
- 共模抑制比：`-110dB`。
- 可编程增益：`1、2、4、6、8、12、24`。
- 模拟供电：`4.75V` 至 `5.25V`。
- 数字供电：`1.8V` 至 `3.6V`。
- 支持内置偏置驱动放大器、持续断线检测、测试信号。
- 支持内部/外部基准、内置振荡器、省电/待机模式。
- 串行接口兼容 SPI。
- 工作温度：`-40°C` 至 `+85°C`。
- 可采用菊花链配置串联多个器件，用于更多通道系统。

注意：

- ADS1299 的数据速率是芯片级能力，不等于当前 APP 演示中的实际采样率。
- APP 中如果展示采样率，建议写“芯片支持 250SPS-16kSPS，实际采样率随通道数与通信模式配置”，避免写死不准确参数。

## 9. 实体照片整理

4 张照片均可作为“真实硬件”证据素材。

建议使用方式：

- `bdf208d721506d921b0955797a92a233.jpg`：适合作为硬件实测首图。画面包含 EEG 脑电帽、蓝色采集盒、电极线、代码界面、脑地形图界面。
- `902a275ac266c9d907bfdafaa8ba5c34.jpg`：适合作为佩戴场景图。受试者侧面、脑电帽和采集电脑较清楚。
- `886fd19810837b17c482e72d38c42306.jpg`：适合证明“硬件 + 线束 + 采集盒 + 软件处理链路”。
- `6d8018b78313f8ffd3ea35c6aef8d41f.jpg`：适合做佩戴细节特写，可用于说明电极点位和帽体。

APP 接入建议：

- 将 2-3 张照片复制到 `public/hardware/`。
- 在 `src/data/hardware.ts` 中配置为：
  - 主图：硬件实测首图。
  - 佩戴场景：受试者侧面佩戴图。
  - 采集证明：包含电脑脑地形图/代码界面的照片。

## 10. SSVEP 演示数据

路径：`EEG files\OpenBCI\mind-speech-interface-ssvep-main\demo_data`

已发现文件：

- `245_2024_086076.csv`，约 8MB。
- `245_2024_086076.txt`，记录试次信息。

CSV 表头：

```text
time,CH1,CH2,CH3,CH4,CH5,CH6,CH7,CH8,Color Code,Frequency
```

测试报告：

- 数据列表尺寸：`(46587, 24)`
- Channel Names：`Fp1, Fp2, C3, C4, P7, P8, O1, O2`
- Channel Numbers：`1, 2, 3, 4, 5, 6, 7, 8`
- Number of Channels：`24`
- Marker Channel：`23`
- Data Acquisition Duration：`186.1675s`
- Expected Samples：`46541.88`
- Received Samples：`46587`

可用于 APP 的表达：

> 已有 SSVEP 演示数据链路，可展示从 EEG 采集、标记刺激频率到后续可视化分析的完整实验流程。

注意：

- CSV 表头显示 8 个 EEG 通道和刺激标记字段；测试报告中的 24 通道应理解为原始处理矩阵或完整数据结构，不宜在 APP 中直接写成“本次 CSV 有 24 个 EEG 电极通道”。

## 11. 运动想象实验代码

路径：`EEG files\OpenBCI\BCI-master\BCI-master`

主要文件：

- `making_data.py`：通过 LSL 获取 EEG 流，采集左手握紧、右手握紧、放松三类动作想象数据。
- `training.py`：使用 TensorFlow/Keras 训练 Conv1D 分类模型。
- `testing_data.py`：加载模型并实时预测动作想象类别。
- `analysisAll.py`：批量测试模型并生成混淆矩阵。
- `split_data.py`：划分训练、验证、测试数据。

当前代码特征：

- 从 LSL 中解析 EEG stream。
- 使用 `FFT_MAX_HZ = 60`。
- 训练输入形状为 `(-1, 8, 60)`。
- 分类目标为 `left`、`right`、`none`。
- 单次采集窗口约 `15s`。
- `making_data.py` 中每个动作可执行 60 次采集。
- `training.py` 使用 Conv1D + Dense 的三分类模型。

当前数据状态：

- `BCI-master\data\left`、`none`、`right` 目录存在，但当前为空。
- `new_models` 和 `pictures` 当前未看到已有模型文件或混淆矩阵输出。

APP 展示建议：

- 可以展示“运动想象算法实验流程已预留”，不要写成“已完成大规模模型训练”。
- 如果比赛演示需要，可以做成“脑机接口拓展能力”：左/右/放松三分类，为未来主动干预或交互控制预留。

## 12. 可直接进入 APP 的硬件展示字段

建议更新 `src/data/hardware.ts` 时使用：

| 字段 | 建议内容 | 依据 |
| --- | --- | --- |
| 设备名称 | ExpandableEEG 脑电采集模块 | 使用说明书 |
| 通道能力 | 8/16/32 通道可扩展 | 使用说明书 |
| 核心芯片 | ADS1299 系列 24 位 EEG/生物电势 ADC | 芯片资料 |
| 通信方式 | WiFi AP、WiFi Client、蓝牙串口 | 使用说明书 |
| 默认 AP | ExpandableEEG AP / 192.168.4.1 | 使用说明书 |
| 数据接入 | OpenBCI GUI、LSL、BrainFlow | 使用说明书 |
| 信号质量 | 支持阻抗/电极接触质量检测，32 通道暂不支持 | 使用说明书 |
| 多模态 | EEG，兼容 EMG 双端输入 | 使用说明书 |
| 采样能力 | ADS1299 支持 250SPS-16kSPS；实际采样率随配置变化 | ADS1299 资料 |
| 续航 | 暂未找到准确数字 | 不要编造 |
| 重量 | 暂未找到准确数字 | 不要编造 |

## 13. APP 创新性展示建议

推荐把硬件展示从“照片陈列”改成“闭环链路”：

1. 硬件采集：学生佩戴 EEG 脑电帽，ExpandableEEG 通过 WiFi/蓝牙输出信号。
2. 信号接入：OpenBCI GUI/LSL/BrainFlow 获取 EEG 数据，监控波形、丢包和电极质量。
3. 状态识别：APP 将脑电特征转化为 Valence、专注度、信号质量等指标。
4. 异常提醒：当出现低效价、走神或信号异常时，界面用颜色和提示即时提醒。
5. 干预推荐：APP 推出呼吸训练、番茄专注、数字处方、AI 陪伴等干预方式。
6. 闭环记录：干预完成后写入个人成长档案，形成“监测-分析-预警-干预-随访”的闭环。

这条线最适合互联网+答辩：

> 我们不是单纯做心理内容 APP，而是把可扩展 EEG 硬件、实时脑电采集、情绪效价识别和个性化数字干预连成闭环。

## 14. 视频演示脚本建议

时长建议：45-60 秒。

镜头安排：

1. 问题场景：学生备考或情绪低落，APP 显示 Valence 低效价提醒。
2. 硬件佩戴：展示 EEG 脑电帽、电极线、蓝色 ExpandableEEG 采集盒。
3. 实时采集：电脑端出现 EEG 波形、脑地形图或 BrainFlow/GUI 采集界面。
4. APP 识别：手机端显示“较低效价/注意力下降/信号质量”。
5. 智能干预：APP 推荐呼吸训练、考研冲刺专注流、AI 陪伴。
6. 闭环结果：完成训练后，Valence 提升，生成成长记录。

视频文案核心句：

> ExpandableEEG 采集脑电信号，智愈 APP 识别情绪效价并推荐干预，让心理状态从“被动察觉”变成“实时监测、及时调节、持续记录”。

## 15. 不建议直接写死的内容

以下内容目前资料中未找到准确数据，不能随便编：

- 整机重量。
- 真实续航小时数。
- 当前项目实际长期注册用户与真实下载量。
- APP 实时模型在真实临床/校园场景中的准确率。
- 32 通道阻抗检测已支持。
- 运动想象模型已经完成大规模训练。

如果比赛页面需要展示这些指标，应标注为“演示数据”或“待接入真实统计”。
